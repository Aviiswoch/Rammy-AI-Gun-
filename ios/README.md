# iOS project

This directory is a native SwiftUI/AVFoundation application definition. Generate the Xcode project on macOS with XcodeGen:

```sh
cd ios
xcodegen generate
open RammyAIGun.xcodeproj
```

The current target is the capability-aware UI/capture shell. Physical external-camera validation and Apple Milestones 9–10 have not been completed.

